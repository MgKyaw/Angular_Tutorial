// import { bootstrapApplication } from '@angular/platform-browser';
// import { AppComponent } from './app/app.component';
// import { config } from './app/app.config.server';

// const bootstrap = () => bootstrapApplication(AppComponent, config);

// export default bootstrap;

// import { createApplication } from '@angular/platform-server';
// import { createCustomElement } from '@angular/elements';
// import { EmpCardComponent } from './app/emp-card/emp-card.component';

// export default function mainServer() {
//   return createApplication({ providers: [] }).then((appRef) => {
//     const empCard = createCustomElement(EmpCardComponent, { injector: appRef.injector });
//     customElements.define('emp-card', empCard);
//   });
// }
