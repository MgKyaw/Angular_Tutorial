import { GreetContentDirective } from './greet-content.directive';

describe('GreetContentDirective', () => {
  it('should create an instance', () => {
    const directive = new GreetContentDirective();
    expect(directive).toBeTruthy();
  });
});
