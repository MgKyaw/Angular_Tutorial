import { DynSampleDirective } from './dyn-sample.directive';

describe('DynSampleDirective', () => {
  it('should create an instance', () => {
    const directive = new DynSampleDirective();
    expect(directive).toBeTruthy();
  });
});
